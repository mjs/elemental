* fix issues with backwards transpose when at start of argument (see failing test)
* clean up tests
* documentation of functions
* the usual description and install instructions at the top of the main .el
