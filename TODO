* elem- to elem/ prefix change
* documentation of functions

* screencast demo

-- first release --

* function for deletion of elements
* make it work in situations where comma isn't the separator (eg. lisp-mode)
* back and forward swap functions that don't use tranpose-subr and
  therefore don't have some of it's oddities?
* avoid repeated code in tests (macro?)
* move comments with elements when transposing multi-line elements (?)
